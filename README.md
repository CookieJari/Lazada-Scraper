# Lazada-Scraper
Test Scraping

Yow this is where e scrape the files.

the function to scrape will be in scraper.py, use the function scrape()
input in the url of choice (only LAZADA works for now), then it will return the price (and if available, the stock)
